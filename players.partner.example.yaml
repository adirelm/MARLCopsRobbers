# ============================================================================
# players.partner.example.yaml — §9 bonus PARTNER-group identity (TRACKED placeholder)
# ----------------------------------------------------------------------------
# Copy to players.partner.local.yaml (GIT-IGNORED via the *.local.yaml rule) and
# fill the PARTNER group's REAL values there. Partner PII (names/IDs/owner slug)
# is PII exactly like ours: it may appear ONLY in the git-ignored local file and
# in the outbound §9 bonus email — NEVER in tracked content (the committed
# bonus_report.redacted.json masks both student blocks + both repo URLs).
#
#   cp players.partner.example.yaml players.partner.local.yaml   # then fill
# ============================================================================

group_name: "<PARTNER GROUP CODE>"    # keys groups/totals_by_group/bonus_claim — agree the EXACT string

github_repo: "https://github.com/<PARTNER-OWNER>/<PARTNER-REPO>"   # github_repo_group_2

students:                              # the partner's §9.4 students_group_2 block
  - role: "A"
    full_name: "<PARTNER STUDENT A FULL NAME>"
    id: "<PARTNER STUDENT A ID>"
  # - role: "B"
  #   full_name: "<PARTNER STUDENT B FULL NAME>"
  #   id: "<PARTNER STUDENT B ID>"
