# A6 plan — canonical reconciliation + fix list (post 19-agent review)

> The 19-agent review (5 Claude + 8 Codex + 2 Gemini) found **strong coverage** but
> **doc-vs-config DRIFT** + a few **V3-coverage gaps** + **technical fixes**. `config/config.yaml`
> is the **FROZEN single source of truth** (it is the most correct artifact). Every doc must
> match it EXACTLY. Apply the fixes below; preserve existing good content.

## PART A — AUTHORITATIVE VALUES (from config.yaml — do NOT contradict)
- **MCP ports:** Cop **8001**, Thief **8002** (`mcp.cop_port`/`mcp.thief_port`). NOT 8101/8102.
- **Action dims:** Cop **a_cop=5** {UP,DOWN,LEFT,RIGHT,PLACE_BARRIER}; Thief **a_thief=4** {UP,DOWN,LEFT,RIGHT}. NO NOOP/STAY head (`env.actions.enable_stay:false`). NOT cop=6/thief=5.
- **Config key paths (use these EXACT paths):** `game.{grid_size,max_moves,num_games,max_barriers,scoring{cop_win,thief_win,cop_loss,thief_loss}}` · `env.{num_cops,move_resolution,capture_on_swap,reward_mode,view_radius_by_grid,obs_channels,obs_scalars,actions.{a_cop,a_thief,enable_stay,enable_barrier},curriculum.{stages,num_cops_by_stage,promotion_threshold,promotion_window}}` · `algo.{name,baseline,double_q,gamma,lr_agent,lr_mixer,batch_episodes,target_update_interval,mixer.{type,embed_dim,include_opponent_utility}}` · `nets.{hidden_dim,gru,conv_channels,encoder_hidden}` · `olora.{enabled,rank,rank_sweep,scale,target_layers}` · `bc.*` · `replay.*` · `selfplay.{window_k,pool_size,update_ratio,rounds}` · `training.{seeds,episodes_per_stage,eps_*}` · `reward.*` · `mcp.*` · `cloud.*` · `gmail.*` · `gui.*` · `paths.*`. **Replace any `marl:`/`mixer:`(top-level)/`actions:`(top-level)/`agent:`/`report:`/`email:`/`scoring:`(top-level)/`view_radius:`(scalar) references with the nested config paths above.** (`algorithm.name`→`algo.name`; `agent.num_cops`→`env.num_cops`; `report`/`email`→`gmail`; scalar `view_radius`→`env.view_radius_by_grid` map.)
- **Algorithm:** **QMIX PRIMARY** (`algo.name:qmix`) + VDN ablation (`algo.mixer.type` toggle) + IQL baseline (`algo.baseline:iql`). Any "VDN primary" wording is WRONG → QMIX primary.
- **Reward magnitudes:** the TRAINING reward uses `reward.*` (capture_bonus 1.0, timeout_penalty 1.0, step_penalty 0.05, distance_weight 1.0, barrier_cost 0.25 — potential-based). The **20/10/5/5 are the §3.4 REPORT scoreboard ONLY** (`game.scoring`), computed by the Scorer, NEVER the RL signal. Do NOT use 20/5/10 as reward values.
- **Self-play params:** `selfplay.window_k:1` (update-ratio tick), `selfplay.rounds:50` (best-response rounds/stage), `training.episodes_per_stage:5000`. Do NOT write window_k=5000.
- **Dec-POMDP N:** **N = the COOPERATIVE COP TEAM** (the thief is folded into transition T; the full game is a POSG — §2.1/eq3). N is NOT {cop,thief}. Value decomposition never crosses the cop/thief boundary.
- **ADR set:** **ADR-0001 … ADR-0014** everywhere (PLAN's fuller set). PRD/TODO that say 0001-0011 → 0001-0014.
- **players placeholder location:** repo-root **`players.example.yaml`** (real values in root `players.local.yaml`, git-ignored). Fix PLAN/TODO refs that say `config/players.example.yaml` → repo root.
- **SOLO:** `students[]` has **exactly 1 entry** (role A). Report JSON-Schema `students` **minItems: 1** (allow 1–3), NOT minItems 2.
- **capture_on_swap = true** is a DELIBERATE, documented rule clarification (BRIEF §3.2 is silent on a simultaneous cop↔thief cell-swap; treating swap-through as capture keeps the pursuit well-posed under simultaneous resolution). Document it in ADR-0004 + flag as the architect's game-rule call. Keep `true`.

## PART B — V3 COVERAGE ADDITIONS (make these EXPLICIT requirements/tasks)
1. **§5 ApiGatekeeper + `config/rate_limits.json` (now exists, versioned).** Add a FR + ADR + TODO task: a single `ApiGatekeeper` (in `src/api/`) with `execute(call)` AND `get_queue_status()`, a per-channel token-bucket from `config/rate_limits.json` (peer_mcp/gmail/prefect_deploy), a **FIFO overflow queue** (no crash on burst), and call logging. ALL outbound egress (peer-MCP HTTP, Gmail, Prefect) routes through it. Add `test_egress_via_gatekeeper`.
2. **§11 Cost.** Add a FR + AC + `docs/COST_ANALYSIS.md` content spec: token-cost breakdown (input/output token counts, $/1M per model, total) for the AI-assisted development + the RL **training-compute** envelope (episodes × stages × wall-clock, local-only) + optimization notes.
3. **§13 ISO/IEC 25010.** Add a FR + `docs/QUALITY.md` mapping ALL EIGHT characteristics (Functional Suitability, Performance Efficiency, Compatibility, Usability, Reliability, Security, Maintainability, Portability).
4. **§9 Sensitivity analysis (distinct from the IQL/VDN/QMIX ablation).** Add a FR + task: a controlled SINGLE-PARAMETER sweep (e.g. `view_radius`, `olora.rank`, `selfplay.window_k`, or `reward.distance_weight`) with each parameter's effect on capture-rate documented + a figure. The algorithm comparison is an ABLATION, not the §9 sensitivity sweep.
5. **§9 Analysis notebook.** Add a TODO task: `notebooks/analysis.ipynb` consuming the **SDK only** (LaTeX Dec-POMDP/QMIX equations, the F1–F6 figures, citations) — currently orphaned in the PLAN tree.
6. **§8 cloud runbook.** Add explicit TODO tasks for the 4 appendix stages: (1) local uv deps, (2) `mcp_server.py` `@mcp.tool` loading OLoRA nets, (3) Prefect token + deploy + public URL, (4) Gmail auto-report.
7. **Per-module test matrix.** Add to TODO: every `src/` module has a matching `tests/.../test_*.py` (V3 §6); not just the global ≥85% gate.

## PART C — TECHNICAL FIXES
- **MCP recurrent session-state.** A decentralized recurrent GRU (eq 8) must carry hidden state z_t across the ~25 `request_move` ticks of a sub-game. So the MCP server holds **per-sub-game server-side session hidden state** (keyed by a session id), reset on `new_sub_game`. Do NOT set `FASTMCP_STATELESS_HTTP=true` for the agent servers (or, if stateless, the obs must replay history — pick **server-side per-session state**). Fix PLAN/PRD/TODO.
- **KPI decomposition evidence (K1).** "QMIX ≥ VDN ≥ IQL" credit-assignment must be evidenced on the **4×4 2-cop scenario** (genuine multi-agent mixer), NOT the degenerate 1-cop 5×5. The 5×5 KPI (K2) = trained Cop beats IQL + the Manhattan heuristic. Fix PRD KPIs.
- **uv-only (no requirements.txt).** Remove any `deploy/requirements.txt`; generate deploy deps via `uv export` / `uv pip compile` or ship `pyproject.toml`. V3 forbids requirements.txt. Fix PLAN/TODO.
- **ONE MCP tool contract localhost==cloud.** Use the SAME tool names + paths (`request_move`/`reveal_location`/`query_opponent`/`new_sub_game`/`send_final_report` in `src/mcp/`) for BOTH localhost Stage 1 and cloud Stage 2. Remove the divergent `src/marl/mcp` `propose_action`/`commit_turn` fork. Fix TODO P8.
- **File-size splits (≤150 LOC).** In the PLAN tree, split the at-risk modules: `src/sdk/sdk.py` (9 methods) → facade + `_helpers`; `src/marl/env/cops_robbers_env.py` (reset/step/state/render) → env + `render_state` separate; `src/marl/env/observation.py` → obs encoder split. Note the split so each file is ≤150 LOC.
- **§9 bonus report keys.** Bonus `sub_games[]` KEEP the §3.5 keys `{id,start,end,moves,winner,scores}` and ADD `cop_group`/`thief_group` (per ex06 §9.4) — do not drop the §3.5 fields. Add to PRD FR + the bonus subject string.
- **§9 bonus rules in PRD.** Add an AC capturing the §9 role alternation (sub-games 1–3: group-1 Cop vs group-2 Thief; 4–6 swapped) + bonus scoring (winner +10 / loser +7 / tie +5 each; counts only if both groups send + `mutual_agreement:true`).
- **Four §5.2 data sources DoD.** Add a TODO DoD enumerating all four: Manhattan-heuristic experts, earlier-policy self-play, random-legal coverage, live CTDE transitions — all into the SAME CentralizedReplayBuffer.

## PART D — per-doc assignment
- **PRD.md:** PART-A key-path/port/action/QMIX/N/ADR-count/solo-schema realignment; PART-B FRs+ACs (§5 gatekeeper, §11 cost, §13 ISO, §9 sensitivity); PART-C KPI(K1/K2)+MCP-session-state+bonus-keys+bonus-rules.
- **PLAN.md:** rewrite the §4 config sample to MIRROR config.yaml exactly (nested keys); ports 8001/8002; reward = config values; self-play params; ADR set 0001-0014; file-size splits in the tree; remove requirements.txt; unify MCP contract; MCP session-state; add ApiGatekeeper component + rate_limits.json; add COST_ANALYSIS/QUALITY/notebook to the doc set + §6 phases.
- **TODO.md:** config key paths; players path (root); ADR count 0001-0014; report-schema minItems 1; add tasks — ApiGatekeeper+rate_limits, COST_ANALYSIS(§11), QUALITY(§13), sensitivity sweep(§9), analysis notebook(§9), §8 4-stage runbook, per-module test matrix, four-data-sources DoD; unify MCP contract; remove requirements.txt.
